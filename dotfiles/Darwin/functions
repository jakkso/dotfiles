 # Open manfile in Preview
 manpdf() {
 man -t "${1}" | open -f -a /Applications/Preview.app/
}

function tojakk {
  if [[ -d $1 ]]; then
    dirFlag="-r"
  else
    dirFlag=""
  fi
scp $dirFlag  -P 45000 $1 lex@192.168.1.64:$2
}

function fromjakk {
  scp -P 45000 lex@192.168.1.64:$1 $2
}
